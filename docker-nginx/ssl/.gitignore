# Gitignore SSL certificates
# Certificates are auto-generated by Docker, but we keep them in git
# to avoid regenerating on every run

# Uncomment below to ignore certificates (recommended for security)
# *.pem
# cert.pem
# key.pem
